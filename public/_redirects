# Cloudflare Pages 路由规则。格式：来源 目标 状态码
#
# 注意：CF Pages 中**静态文件优先于本文件的规则**，已存在的文件无法在此屏蔽。
# 因此"哪些文件对外可见"由目录边界决定：只有 public/ 会被发布，
# docs/ tools/ CLAUDE.md 等仓库内产物天然不在站点里。

# 未知路径回首页，而不是 CF 默认 404 页
/*   /index.html   200
