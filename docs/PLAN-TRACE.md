# 方案追溯矩阵 · PLAN TRACE

方案 v1.0 条款 → 代码落点。验收时按本表逐行核对。
状态：**✅ 已实现** / **◐ 部分**（说明缺口）/ **○ 未开始**

---

## 02 设计定位与原则

| 条款 | 状态 | 落点 |
|---|---|---|
| 原则 01 证据优先：每个观点关联观测/数据/模型/论文/成员 | ✅ | `content/index.js` 反向关联索引；代表性发现面板逐项列出论文/站位/航次 |
| 原则 02 科研可读：术语准确 + 图形降低门槛 | ✅ | 每个图件带单位、变量、图注；剖面图附完整数据表 |
| 原则 03 克制动态：动效必须对应流动/传播/混合/观测 | ✅ | 粒子=平流、波束=内潮传播、下潜=深度尺度；禁止项见 CLAUDE.md |
| 原则 04 内容可维护：页面由结构化内容生成 | ◐ | 列表全部由 `content/` 生成；**首屏与版块抬头文案仍在 HTML 与 content 各一份**（ADR-008） |
| 原则 05 双语同构：同一内容模型 | ◐ | 同一 `{zh,en}` 模型贯穿全站，切换覆盖整页；**hreflang 与中英独立 URL 待 Astro 阶段** |

## 03 品牌概念

| 条款 | 状态 | 落点 |
|---|---|---|
| Logo：V 形等深线 + 三条流线 + 垂直刻度轴，线性矢量 | ✅ | `index.html` 内联 SVG `.brand-mark` |
| 命名分层：中文正式名 / 英文建议名 / OCOM Lab | ✅ | `content/site.js` `BRAND`，含 `nameStatus: 'proposed'` 标记待确认 |

## 04 视觉系统

| 条款 | 状态 | 落点 |
|---|---|---|
| 七个色彩令牌 | ✅ | `styles/tokens.css`；`scenes/palette.js` 运行时读取，canvas 与 DOM 同源 |
| 字体分层：中文黑体 / IBM Plex Sans / IBM Plex Mono（参数与坐标） | ✅ | `tokens.css` 字体栈；全部科学量走 `.mono` |
| 正文 16–18px、行高 1.6–1.75 | ✅ | `--fs-body: 17px`、`--lh-body: 1.7` |
| 固定母题：等深线、流场粒子、水柱剖面、经纬度与深度刻度 | ✅ | `scenes/layers/*`、`figures/*` |
| 科研图必须显示单位/变量/数据来源 | ✅ | `.figure-note` 为必需元素；场景图注常驻 |
| 卡片圆角 4–8px、弱阴影、细描边、不做卡片套卡片 | ✅ | `components/card.css`，卡内无嵌套 `.card` |
| Thermal Coral 仅用于异常值/重点/警示，占比 < 8% | ✅ | 仅用于内潮波束、流向反转线、占位标记 |

## 05 信息架构

| 条款 | 状态 | 落点 |
|---|---|---|
| 七个一级栏目及其"核心问题" | ✅ | `content/site.js` `NAV`（含 `ask` 字段，导航悬停显示） |
| 论文按年份/方向/海域筛选 | ✅ | `ui/publications.js` 三维筛选 |
| 数据、模型、论文、成员、航次双向关联 | ✅ | `content/index.js` `buildIndex()` / `related()` |
| 搜索结果按内容类型分组 | ✅ | `content/index.js` `search()` + `ui/overlays.js` |

## 06 首页体验

| 模块 | 状态 | 落点 |
|---|---|---|
| 01 顶部导航：首屏透明、滚动转雾白 | ✅ | `components/header.css` `.is-scrolled` |
| 02 Hero：海沟地形 + 流场粒子 + 水深刻度，无高频轮播 | ✅ | `scenes/trench-scene.js` 八图层 |
| 03 发现标签：用户主动切换三个专题 | ✅ | `ui/discovery.js` → `SCENE_STATE` → 场景三态 |
| 04 科研身份带：稳定标签，不堆易过期数字 | ✅ | `content/site.js` `IDENTITY` |
| 05 代表性发现：左交互剖面 + 右问题/方法/发现/入口 | ✅ | `figures/velocity-profile.js` + `ui/discovery.js` |
| 06 四大研究方向 | ✅ | `content/research.js` + `ui/research.js` |
| 07 科研流程 Observe→Data→Model→Explain→Outputs | ✅ | `content/site.js` `WORKFLOW` |
| 08 观测海域地图 | ✅ | `figures/station-map.js`（简化岸线示意图，ADR-006） |
| 09 论文与团队 | ✅ | `ui/publications.js` / `ui/people.js` |
| 首屏交互约束：不自动切换、粒子缓慢、移动端静态、图形标注来源 | ✅ | ADR-004；窄屏与 reduced-motion 走静态流线 |

## 07 关键内页模板

| 条款 | 状态 | 落点 |
|---|---|---|
| 观测航次详情（本项目最重要的新增资产） | ◐ | 航次**卡片**已含日期/海域/水深/平台/仪器/站位/关联论文；**独立详情页未建**（demo 只做首页） |
| 研究方向详情 / 数据集 / 模型 / 论文 / 成员 详情页 | ○ | 数据模型已就位，页面待 P3 |

## 08 交互与动效

| 条款 | 状态 | 落点 |
|---|---|---|
| 流场粒子平流 → 降级静态流线图 | ✅ | `layers/particles.js` / `layers/streamlines.js`（静态图**必带箭头**） |
| 等深线、剖面、潮汐波束、潜标、航迹 | ◐ | 等深线/剖面/波束/潜标 ✅；**航迹按时间绘制未做** |
| 禁止项（自动轮播、光斑、3D 海浪、强视差、全体同时淡入） | ✅ | 逐条规避；渐显按元素阶梯触发 |
| 技术建议：轻量 Canvas + SVG/D3 + MapLibre | ◐ | Canvas ✅、SVG ✅；**MapLibre 暂缓**（ADR-006） |

## 09 响应式与无障碍

| 条款 | 状态 | 落点 |
|---|---|---|
| 桌面 12 列 / 最大宽 1280–1360 | ✅ | `--container: 1320px` |
| 手机 <768 单列、16–20px 边距、关闭复杂动画 | ✅ | `layout.css` + `--gutter: 1.125rem` |
| 移动端地图改为重点站位列表 | ✅ | `ui/expeditions.js` 按 `isNarrow()` 切换形态 |
| 对比度达标 | ✅ | `tools/check-contrast.mjs` 27 组令牌全通过；页面审计 95 → 0（V-004） |
| 键盘可操作 + 清晰焦点 | ✅ | 焦点环 2px；剖面图方向键读数；站位可聚焦；浮层焦点陷阱 + Esc |
| 复杂科研图提供文字摘要与数据说明 | ✅ | 剖面图附数据表；所有图件带 `aria-label` 与图注 |
| 支持 prefers-reduced-motion | ✅ | `core/env.js` → `MOTION_CHANGE`，实测降级正常（V-003） |
| 中英文正确语言属性 | ✅ | `i18n/locale.js` 同步 `<html lang>` |

## 10 技术落地

| 条款 | 状态 | 落点 |
|---|---|---|
| 低 JavaScript 负担 | ✅ | 零运行时依赖、零构建；地图按需动态 import |
| 脚本失败时内容仍可读 | ✅ | HTML 语义骨架 + `<noscript>` 提示 |
| 构建期校验缺失字段/重复 DOI | ◐ | `content/schema.js` 已实现校验，运行时在控制台报出；**未接入 CI** |
| 图片 WebP/AVIF 多尺寸 | ○ | 当前无位图，全部为程序生成矢量 |
| SEO：标题、摘要、结构化数据、站点地图、中英 hreflang | ◐ | title/description/OG/JSON-LD ✅；**sitemap 与 hreflang 待正式域名** |
| Astro + TypeScript | ○ | 当前为原生 ESM，结构按可平移设计（ADR-003） |

## 11–12 内容迁移与验收

| 条款 | 状态 | 说明 |
|---|---|---|
| 人员、论文、DOI、联系方式核验 | ○ | **全部为占位**，页面常驻提示条（ADR-005） |
| 素材清单（航次照片、方向示意图、视觉摘要、肖像、航迹） | ○ | 待研究室提供；当前用程序生成矢量占位 |
| P0 内容盘点 → P4 上线验收 | ◐ | 本 demo 对应 P2「关键原型」阶段 |

---

## 缺口汇总（下一阶段优先级）

1. **观测航次详情页** —— 方案 §12.1 指定的第一阶段三个页面之一，目前只有卡片。
2. **研究方向详情页** —— 同上。
3. **内容迁到 Astro** —— 一并消除 ADR-008 的文案双份问题，并补齐 hreflang / sitemap。
4. **航迹按时间绘制** —— 方案 §08 动效表中唯一未实现项。
5. **MapLibre 替换示意地图** —— ADR-006 约定的 P3 动作。
6. **真实素材接入** —— 方案 §11 素材清单，阻塞上线。
